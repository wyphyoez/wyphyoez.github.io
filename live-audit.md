# Live Website Audit — Wai Yan Phyoe (@wyphyoez)

The deployed Cloudflare Pages homepage is reachable at `https://wyphyoez.pages.dev/` and returns a successful page response. The live page correctly shows the identity **Wai Yan Phyoe (@wyphyoez)** and uses a large gradient headline, a compact header, a dark-mode switch, a Resume link, a hero scroll cue, education cards, project cards, and a footer.

The strongest existing elements are the clear identity, the large typographic hero, and the simple route structure. The highest-impact weaknesses are that the Resume link still points to `https://google.com`, project cards reuse the same HooBank thumbnail for Astro Portfolio, the article route appears very bare, the hero has excessive empty vertical space before the next content block, social links still use the old `waiyanphioe` handle, project-card icon labels are generic `Social Media`, and the homepage copy is broad rather than outcome-oriented.

The upgrade should preserve the SvelteKit architecture and existing routes while improving content hierarchy, project credibility, route discoverability, contact/Resume trust signals, accessibility labels, and responsive rhythm. The design should remain recognizable rather than replacing the original identity wholesale.

## Local Upgrade Verification

The upgraded homepage now has a compact rounded navigation bar, a stronger dark-mode composition, a clear role label, distinct full name and handle, actionable hero CTAs, and a live-location card. The main content rhythm is much tighter than the original and the project cards now expose real source/live-preview labels.

The upgraded `/articles` route renders correctly with a field-notes header, numbered article rows, previews, dates, reading times, and pagination controls. The existing seed articles still include generic template content such as “Getting Started” and “Lorem Ipsum”; this is now clearly visible as a content-quality issue rather than a layout issue. Those articles should be replaced or rewritten in a future content pass using Wai Yan Phyoe’s actual notes.

The local article check exposed one remaining duplicate template entry: the original `articles/getting-started/index.md` file is still being globbed alongside the rewritten `articles/getting-started.md`. It should be removed or renamed so the public index contains only the three rewritten Wai Yan Phyoe notes.

Screenshot-driven revision: the Hero now uses a dark high-contrast panel with a mobile-safe type scale, full-width stacked CTAs, and the corrected `ask@wyphyoez.cyou` action. The Footer now uses a responsive identity/navigation grid, visible contact pill, two-column mobile links, and a separated metadata row to prevent clipping and awkward wrapping.

Visual verification: at 390px the Hero now presents a dark framed panel, fully stacked CTAs, readable `Wai Yan Phyoe` wrapping, and a contained Currently card without horizontal overflow. At desktop width the Hero resolves into a balanced two-column composition with the contextual card aligned to the right. The Footer source now uses a mobile-safe identity/navigation grid and email pill; it is ready for the same production deployment.

Responsive recovery findings: the mobile menu control is present in the DOM and toggles its `aria-expanded` state with the Svelte click handler. The regression was caused by mixing the repository’s custom max-width `md` breakpoint with the newly introduced min-width aliases; the Header now uses `minmd:hidden` for the mobile control and panel, while the desktop navigation remains `minmd:flex`. A 390px local screenshot confirms both theme and hamburger controls are visible.

Rich Hero/Footer visual verification: the mobile Hero now clearly separates role, independent-builder identity, name, handle, summary, location, focus, availability, and two CTAs without horizontal overflow. Desktop resolves into a balanced two-column composition with the contextual status card aligned to the right. The Footer now closes with a stronger collaboration invitation, verified email CTA, categorized Explore/Read links, location, availability, identity, deployment stack, and year.

Header/logo verification: the fixed Header now has a clear gap above the Hero on both 390px and desktop screenshots, so it no longer overlays the Hero content. The new inline M-style brand mark is visible beside `@wyphyoez`, while the theme toggle and mobile menu are equal 40px circular controls with consistent alignment and focus treatment.

Header/articles refinement verification: at the top of the mobile homepage the Header is transparent over the page background, the brand mark remains visible, and the Hero begins cleanly below it. The mobile article index now has a clearer field-notes header, reading-list context card, larger row titles, readable previews, and icon-led metadata without the previous arrow-character treatment.

Article detail/index verification: mobile article detail now has a clear Back to articles link, compact Field note label, readable title, icon-led date/read-time metadata, preview paragraph, and a narrow reading column. Desktop article index resolves into a balanced header/context card and spacious reading rows with metadata and circular arrow icons. The Header top state remains transparent on these light pages while the brand mark stays visible.

Cross-device responsive review — homepage: At 390×844, the mobile Header shows the brand mark/handle plus circular theme and menu controls; the hero card has readable type, stacked metadata, and full-width CTAs without horizontal overflow in the captured viewport. At 768×1024, the navigation switches to the inline desktop-style menu, the hero content widens into a single-column tablet composition, and the availability card remains below the CTA block rather than forcing a cramped two-column layout. The tablet capture shows comfortable line lengths and no visible clipping.

Cross-device responsive review — Articles: At 390×844, the page stacks the field-notes label, title, lede, reading-list card, and article rows with readable spacing; metadata begins near the lower viewport edge without causing horizontal overflow. At 768×1024, the same content remains a single-column tablet layout with a wide reading-list card, while article rows introduce the numbered left rail and circular action control at the right. Titles and previews remain readable, and the row separators maintain a clear rhythm.

Cross-device responsive review — article detail and Projects: The automated detail capture used `/article/making-a-small-site-feel-finished`, which is not the deployed route slug; it correctly rendered the site’s “Post not found” fallback, so the detail layout needs a second capture using a slug from the live Articles links before it can be marked as passed. At 390×844, Projects presents a readable stacked introduction and a full-width HooBank card; the card image scales to the viewport, the badge and SVG action buttons remain inside the image bounds, and the title/number block begins cleanly below the image.

Cross-device responsive review — Uses and wide desktop: At 390×844, Uses keeps the workbench heading and lede readable, then stacks tool groups with pill tags that wrap naturally within the viewport. At 1440×1000, Articles uses a restrained max-width, a two-part header/context composition, and spacious rows; the action icons stay aligned at the far right while the reading text remains within a comfortable measure rather than stretching across the screen.

Cross-device responsive review — valid article detail: The live Articles index exposes `/article/getting-started` as the first article route. At 390×844, the valid detail page keeps the Back link, title, date/read-time metadata, preview, divider, and body copy within the viewport with no visible horizontal clipping; the title wraps to two lines and the reading measure remains comfortable. At 1440×1000, the detail page uses a restrained editorial column, a large single-line title, compact metadata, and readable body width with the next-note navigation beginning below the reading content. The earlier “Post not found” result was a test-input issue, not a responsive layout failure.

Cross-device responsive review — About: At 390×844, the About headline wraps into a strong four-line mobile composition, the long-form introduction remains readable with generous line spacing, and the value-card section begins below the fold without visible horizontal overflow. At 768×1024, the headline scales up substantially while maintaining controlled line breaks, the introduction spans a comfortable measure, and the value cards stack vertically with consistent rounded containers and spacing.

Mobile theme/menu interaction review: At 390×844 on `/about`, the menu opens cleanly in dark mode with a dark panel, white navigation labels, a visible close icon, and a high-contrast GitHub action. Switching to light mode while the menu remains open updates the Header handle, moon icon, and close icon to dark tones and renders the menu as a white panel with dark labels and a dark GitHub action. Both captured states remain inside the viewport without visible horizontal overflow or clipped touch controls.

Mobile Header alignment polish: At 360×844, the brand mark and the theme/menu controls now share a consistent 44px visual size and align cleanly on the Header baseline in both dark and light states. The touch controls use a neutral pressed-scale response with the browser tap highlight suppressed; blue outlines remain reserved for keyboard `:focus-visible` navigation rather than appearing on ordinary touch/click activation.

Logo boundary and identity correction: At 360×844 with the mobile menu open, the logo now has the same 44px circular boundary sizing and comparable border/background treatment as the theme and menu controls in both dark and light modes. The control row remains aligned without clipping. The hero email CTA now displays `ask@wyphyoez.cyou`, and the homepage closing CTA uses the same centralized email value; the canonical name remains `Wai Yan Phyoe` and the handle remains `@wyphyoez`.

Mobile contact and interaction audit: The site does not implement a submitted contact form; it intentionally uses `mailto:ask@wyphyoez.cyou` actions. At 360px, every tested contact action on About, Home, and Footer used that canonical address. The mobile menu opened and navigated to Projects, the theme control toggled light/dark state in both directions, project source/preview links exposed valid destinations, and Articles exposed article rows, back navigation, and next-note navigation. No mobile interaction defect was found.

Shared Header boundary refinement: At 360×844, the mobile Header now has a single rounded outer surface with equal six-pixel insets around the brand/handle group and theme/menu group. The logo, theme button, and menu button share the same 44px size and theme-aware bordered surface. Both dark and light states retain readable text/icons, and the visible contact CTAs now display only `ask@wyphyoez.cyou`.

Mobile dropdown refinement review: The new menu hierarchy keeps a Header-aligned panel, navigation label, grouped rows, and separated GitHub CTA. Light mode rendered the intended clean white surface at 360px. During the final dark-mode capture, the opaque-surface experiment showed page-content bleed-through beneath the menu, so the dark panel needs one further explicit-background correction before it is marked complete.

Mobile dropdown final verification: The panel now uses explicit solid theme backgrounds, eliminating dark-mode page-content bleed-through. At 360×844, both dark and light open-menu states preserve the shared Header spacing, a clear Navigation label, readable row rhythm, a visually separated GitHub CTA, and clean viewport bounds.

Transparent/Header surface verification: At 360×844 in dark mode, the pre-scroll Header computed to a fully transparent background and the scrolled Header computed to the intended dark surface. Visual captures confirm that the Header has no enclosing surface at the top and gains its bordered glass treatment only after scrolling.

Matched Header/dropdown surface verification: At the scrolled position with the menu open, the Header and dropdown both computed to `rgb(24, 24, 27)` in dark mode and `rgb(255, 255, 255)` in light mode. The paired captures confirm aligned boundaries and matching theme surfaces while retaining the required transparent top state.

Social action and touch-focus review: The mobile dropdown now displays GitHub, LinkedIn, Instagram, and X as four icon actions that link to the verified `wyphyoez` profiles. Both dark and light captures show a balanced four-column social row. The scripted mobile action audit confirmed all four accessible profile destinations and no visible outline or box-shadow after the menu-control click.

Social action final verification: Each of the four icon actions measures 48px in height at the 360px viewport, producing appropriately sized mobile touch targets. Dark and light menu captures preserve readable icon contrast and stable spacing; touch activation of the menu control reports no outline or box-shadow.

Cross-section responsive review — mobile Footer: At 360×844, Home and Projects both show the email CTA inside the available width, stable two-column Explore/Read navigation, a full-width Now block below those links, and a correctly stacked attribution row. No horizontal overflow or clipped Footer controls appeared. The fixed Header overlays the uppermost pixels of a footer-bottom capture only because the capture was taken at the absolute scroll limit; the footer itself remains scrollable and structurally intact.

Cross-section responsive review — tablet Footer: At 768×1024, both Home and Articles transition to a readable three-column Explore/Read/Now arrangement. The heading, supporting copy, email CTA, dividers, and closing attribution line maintain comfortable measure and alignment. The captured content did not exceed the viewport width.

Cross-section responsive review — desktop Footer: At 1440×1000, Home and Projects retain the intended two-column editorial composition: an ample left CTA block and compact right navigation/status grid. The closing row is balanced across the available width without stretching text measure or clipping the CTA and links.

Cross-section responsive review — mobile Hero and Projects: At 360×844, the Home hero retains clear headline wrapping, a readable summary measure, vertically ordered location/focus/availability metadata, and an in-bounds primary CTA. The Projects route maintains an intentional single-column reading order, with the project image confined to its rounded card and the opening copy wrapping without overflow.

Cross-section responsive review — mobile Articles and Uses: At 360×844, Articles maintains a readable heading, supporting copy, Reading List card, separator rhythm, and article summary without crowding. Uses retains a concise two-line heading and keeps tool pills on clean wrapped rows. Neither route introduced horizontal overflow or truncated primary controls.

Projects/Uses visual refinement review: The refined mobile Project card has a balanced image frame, explicit case-study metadata, contained action controls, and a stronger title/tag hierarchy. Initial screenshots revealed that the capture harness accumulated theme-init scripts and therefore did not reliably switch to light mode; the harness needs correction before cross-theme findings can be finalized.

Projects/Uses visual refinement review — Uses: At 360px, the redesigned tool-group cards establish a clear numbered hierarchy, group count, separator, and tool-pill row. Pill wrapping remains within the card boundary. The current capture still reports dark styling for the nominal light mode; a reload-based harness correction is being applied before final cross-theme signoff.

Projects/Uses final mobile light-mode review: At 360×844, Projects has clear editorial hierarchy, a contained image frame, and clean separation between the visual and project details. Uses retains its numbered group structure, count badge, divider, and readable wrapped pills inside the card boundary. The Header’s fallback logic now follows the system preference when no explicit choice is stored; the document renders a white light-mode surface after reload while explicit dark mode remains intact.

Projects/Uses final desktop review: At 1440×1000, Projects resolves into a calm two-column grid with equal visual weight, framed project imagery, concise metadata, and in-card actions. Uses forms a balanced two-column set of tool-group cards; numbered labels, count badges, dividers, and pill rows retain a clear scan order. Final automated checks confirm light mode renders on a white document surface and explicit dark mode renders on the intended dark surface at both phone and desktop widths.

TinyLife local verification: The Projects grid now begins with TinyLife, a private memory-focused mobile application presented with verified React Native, Expo, and Supabase tags. The card uses a neutral abstract cover rather than a task screenshot containing staging-only state or personal family imagery. Because the TinyLife source repository is private and there is no public product route approved for linking, the card deliberately omits public source and live-preview actions while retaining the accessible actions for projects that have public links.

TinyLife production verification: Cloudflare Pages deployment `03648701` completed successfully for commit `1a7b689` and is aliased to `https://wyphyoez.cyou`. The live Projects route displays TinyLife first, its private-memory app label, verified description, and React Native/Expo/Supabase tags. The page exposes no public source or live-preview controls for TinyLife, while the externally linked projects retain their actions.

TinyLife image-fix verification: The original `/manus-storage` reference returned 404 on Cloudflare Pages. The generated privacy-safe cover was uploaded to a verified public CDN URL, the Projects data was updated, and deployment `94b49235` for commit `334740d` completed successfully. The live asset returns HTTP 200 as a 4.6 MB PNG, and `https://wyphyoez.cyou/projects` visibly renders the TinyLife cover with the card title and technology tags intact.

Ordered redesign mobile review: At 360 px, the refreshed homepage preserves a clear editorial hero, visible primary CTA, readable metadata, and a contained mobile header. The About route retains its card and footer rhythm without horizontal overflow. Automated layout metrics show no horizontal overflow for the updated home, Articles, About, and TinyLife case-study routes at this width.

Ordered redesign desktop review: At 1440 px, the TinyLife case study maintains a balanced two-column introduction, concise role/year metadata, and a large, contained cover image. The Articles archive keeps an editorial hierarchy with readable excerpts, date and reading-time metadata, and quiet topic tags. Automated layout metrics show no horizontal overflow across the same updated routes at desktop width.

About collaboration review: The desktop About page places the collaboration-fit section directly after the personal working principles. Its three cards are aligned, text remains legible, and the availability CTA stays clear before the footer. The section also stacks within the mobile layout without recorded horizontal overflow.

<!-- The public CDN URL is intentionally used because the Cloudflare Pages deployment did not serve the project-scoped /manus-storage path. -->
